<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %><g:each in="${tradeWithdrawnInstanceList}" status="i" var="tradeWithdrawn">${tradeWithdrawn.customerBankAccountNo}|${tradeWithdrawn.customerBankAccountName}|${tradeWithdrawn.amount?tradeWithdrawn.amount/100:0}|<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>