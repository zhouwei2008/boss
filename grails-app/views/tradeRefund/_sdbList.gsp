<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %><g:each in="${tradeRefundInstanceList}" status="i" var="tradeRefund">${tradeRefund.outTradeNo}<g:formatDate date="${TradePayment.findByRootIdAndTradeType(tradeRefund.rootId,'payment')?.dateCreated}" format="yyyyMMdd"/>${tradeRefund.refundHandleName}${channel}${channel==1?"个人退款":"企业退款"}<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>