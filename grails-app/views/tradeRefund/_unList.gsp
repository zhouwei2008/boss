<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %>${totalCount}|${totalAmount?totalAmount/100:0}<%="\r\n"%><g:each in="${tradeRefundInstanceList}" status="i" var="tradeRefund"><g:formatDate date="${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.dateCreated}" format="yyyyMMdd"/>|${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.outTradeNo}|${tradeRefund.amount?tradeRefund.amount/100:0}<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>