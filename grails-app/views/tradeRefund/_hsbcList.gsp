<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %>总笔数：${totalCount}   总金额：${totalAmount?totalAmount/100:0}<%="\r\n"%>订单号   日期   金额(元)   备注 <%="\r\n"%>------------------------------------------------------------------<%="\r\n"%><g:each in="${tradeRefundInstanceList}" status="i" var="tradeRefund">${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.outTradeNo}    <g:formatDate date="${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.dateCreated}" format="yyyyMMdd"/>    ${tradeRefund.amount?tradeRefund.amount/100:0}    ${tradeRefund?.note}<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>