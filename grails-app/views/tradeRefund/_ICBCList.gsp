<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %><g:each in="${tradeRefundInstanceList}" status="i" var="tradeRefund">${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId, 'charge')?.outTradeNo},<g:formatDate date="${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId, 'charge')?.dateCreated}" format="yyyyMMdd"/>,${tradeRefund.amount},${tradeRefund?.note}<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>