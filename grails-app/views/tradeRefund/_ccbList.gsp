<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %><g:each in="${tradeRefundInstanceList}" status="i" var="tradeRefund">${i+1}|${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.outTradeNo}|12001615300052524695|0|${tradeRefund.amount?tradeRefund.amount/100:0}|<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>