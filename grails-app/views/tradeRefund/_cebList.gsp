<%@ page import="boss.AppNote; boss.BoAcquirerAccount; boss.BoBankDic; ismp.TradeBase; gateway.GwTransaction; ismp.TradeCharge; ismp.TradePayment; ismp.TradeRefund" %>${totalAmount?totalAmount/100:0}<%="\r\n"%>${totalCount}<%="\r\n"%><g:each in="${tradeRefundInstanceList}" status="i" var="tradeRefund">${tradeRefund?.tradeNo},<g:formatDate date="${new Date()}" format="yyyyMMdd 00:00:00"/> ,,${tradeRefund?.amount?tradeRefund?.amount/100:0},156,${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.outTradeNo},<g:formatDate date="${TradeBase.findByRootIdAndTradeType(tradeRefund.rootId,'charge')?.dateCreated}" format="yyyyMMdd"/>,退款<g:if test="${totalCount>(i+1)}"><%="\r\n"%></g:if></g:each>